#include "main.h"

/**
 *mul - returns the product of two ints
 *@a: an int given by the main function
 *@b: an int given by the main function
 *
 *Description: Uses header to link and a nested loop to achieve goal
 *Return: see first line
 */

int mul(int a, int b)
{
return (a * b);
}
